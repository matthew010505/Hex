<div class="buttons-wrapper">
     <a href="/" class="logo">HEX</a>
     <div   class="buttons">
        <a href="/new" class="btn">New</a>
        <% if (locals.canSave) { %>
        <button class="btn" type="submit">Ctrl + S</button>
            <% } %>
        <% if (locals.id!=null) { %>
            <a href="/<%= locals.id  %>/duplicate" class="btn">Duplicate</a>
                    <% } %>
       
     </div>
</div>